<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mineswipper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.afeka.sm.Minesweeper.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.afeka.sm.Minesweeper.SecondActivity"
            android:label="Mineswipper"
            android:theme="@style/AppTheme"
            />

        <activity
            android:name="com.afeka.sm.Minesweeper.ThirdActivity"
            android:label="MineSwipper"
            android:theme="@style/AppTheme" />
    </application>

</manifest>